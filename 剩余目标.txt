battle 3.0v actor ability attr 

actor：
1.actor 及 actorlogic actorModel 要求尽可能的无GC，使用APool减少GC
2.actor的使用调试要简单，要求拖出来就可以用（属性查看器，技能学习，buff查看，攻击盒查看，检测距离查看）
3.layer管理
4.入场流程优化，当帧入场，当帧废弃，下帧删除，废弃后不参与任何判定，死亡与废弃是不同的状态
5.简单技能模式
6.简单Buff模式

ability：
1.接入APool，优化调用流程，反射改为预生成代码直接调用，传递使用引用对象，减少装箱拆箱
2.流程优化，当帧执行，当帧中止，pre -> Executing在同帧，Executing->end也是同帧，确保ability结束时一定会调用end
3.ability函数调用实现重载调用
4.更细致的异常捕获
5.ability其实不应该去区分运行不运行，只有执行和停止

ability编辑器优化：
1.新增setting页面，node长度，node颜色，显示方式，保存设置,路径管理收集
2.node复制粘贴，节点限制
3.重载存入，复杂数据配置保存，


ability的消耗点主要在哪里：
2.计算函数参数传递时的装箱拆箱{
    AParam 如果只是存入简单值，则只会有拆箱和转换的消耗
    如果是属性，变量，函数计算应当直接返给函数参数，而不是塞到object列表里，否则和直接用反射有什么区别呢
    变量容器存放池对象会增加一次引用计数，对象池增加引用计数消除
}


event节点下面是否可以有计时器

setVariable 来源-> AParams -> 属性，直接输入，函数返回值 -> 最好的处理方式是引用计数？


ability中VarNode的目的是持有一个引用



ability在回收时会清理variable



actor在回收时会清理variable



一个问题：对于配置，我需要存储的是引用数据，这样才能保证存入数据在使用时不会产生装箱拆箱，
但是在使用object时对于一些数据的处理很麻烦，比如深拷贝，比如不可控

引用计数问题