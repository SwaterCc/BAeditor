battle 3.0v actor ability attr 

actor：
1.actor 及 actorlogic actorModel 要求尽可能的无GC，使用APool减少GC
2.actor的使用调试要简单，要求拖出来就可以用（属性查看器，技能学习，buff查看，攻击盒查看，检测距离查看）
3.layer管理
4.入场流程优化，当帧入场，当帧废弃，下帧删除，废弃后不参与任何判定，死亡与废弃是不同的状态
5.简单技能模式
6.简单Buff模式？

		//位移行为是否可以穷举 -> 跟随位移（方向锁定目标），，），速度采用曲线，方向-指定方向，跟随目标，
		//是否存在单独的旋转？->特殊技能需要旋转释放
		//转身速度
		//匀速位移 -》完全向指向目标方向移动 ，旋转和位移会相互影响很麻烦

		//ActorAction-》ActionSystem -》SpeedChange，Rotation, Sequence，Spawn，ModelJump,PlayAnim


		//变速位移（速度曲线） 方向位移（指向方向,锁定方向) 旋转（仅旋转，指向目标旋转） -》 AAction唯一
		// Ability ActionNode-> FunctionNode


//每个Actor身上添加特殊命中点
//之前值类型不好处理，但是可以转换为同一种值类型
	[Serializable]
	public class AutoValue{
		//类型名
		public string typeName;
		//是否为引用类型
		public bool isRef;
		//简单值类型存储
		public byte[] bytes;
		//引用类型存储
		public object Ref;
	}
	

ability：
1.接入APool，优化调用流程，反射改为预生成代码直接调用，传递使用引用对象，减少装箱拆箱
2.流程优化，当帧执行，当帧中止，pre -> Executing在同帧，Executing->end也是同帧，确保ability结束时一定会调用end
3.ability函数调用实现重载调用
4.更细致的异常捕获
5.ability其实不应该去区分运行不运行，只有执行和停止
6.ParamParser还需要优化
7.直接在函数模板上限制不允许使用值类型，这样可以解决传递引用次数的问题，解决很多小问题，
8.变量黑板，和参数容器（初始化流程中存在，初始化结束后回收）
ability编辑器优化：
1.新增setting页面，node长度，node颜色，显示方式，保存设置,路径管理收集
2.node复制粘贴，节点限制
4.更细致的限制

//////////////////////////////////////////////////////////////////////////////


战场	-战场流程
------------------------------------------
	-地图	-地图数据
		-行军路线
		-阵地构筑
		-排兵布阵
------------------------------------------
	-Actor	-玩家操控单位
		-队友
		-士兵/野怪
		-Boss
		-战场建筑
		
	-游戏系统	-技能
		-Buff
		-投射物
		-Ability系统		

1.Actor为游戏运行的基础单位，与场景脱钩，要求可以独立运行
2.单位原型的构成由配置（json？）定义，同一个原型，因为属性数值的不同可能会产生不同的单位，
3.组件的不同参数导致会产生不同的原型？组件是否需要参数？

玩家单位：不需要配置

其他单位：角色，精英怪物，载具，小兵   （模型数据，逻辑结构）组成

数据来源，玩家，养成角色来自于外部养成数据，怪物来自于固定配置
单位构成，玩家独一份
	其他小兵，角色，载具，巨像，--3C的变换

机枪 -》 


战场 ：
1.亚顿之矛
2.巨像
3.伤害统计，仇恨累计，刷怪频率变化


3.表id对应原型，


任务

1.ActionSystem，指定方向速度
2.Actor周期调整，属性初始化上提到Actor层，可传入Actor
3.属性继承流程，属性链，属性事件更新，减少update的逻辑
4.子弹，打击点，仍然是Actor，其创建时直接拷贝归属对象的属性快照
5.属性继承规则
3.Actor Json组件装配，属性初始化流程改变，能从外部传入，属性初始化改到外部（？）
4.Ability函数导出脚本


4.Skill运行态，Buff运行态，Bullet运行态


6.Power编辑器功能优化，variable提示，事件提示，消息提示，foreach提示，编辑器数据生产

3.池对象List，池对象Dictionary


node是一套父子关系流程，Unit是游戏单位流程
node是游戏的最上级单位

node先更新
地图再更新



        //什么是hitbox？
        //本质是一次检测 + 伤害计算的流程
        //Hit的配置和类型决定了如何检测
        //damage则是有单独的计算
        
        //HitBox 规定了检测方式
        //检测多久，检测几次，比如我有个火焰会对范围内的敌人造成多次伤害，多次来自与配置
        //我发起了一次攻击，这个攻击是否命中，命中后是否产生伤害，HitBox是一个独立的对象，决定产生几次命中检测
        //看起来hitBox更像是一个单独的机制
        //但是，碰撞伤害如何实现？
        //如果我车辆运行时创建了一个绑定打击盒，我车辆停止时 -死亡对象不允许被命中
        //CreateHitBox -> Tick -> Hit -> 命中存活目标 -> damage 传入 攻击者属性， 来源数据（技能，buff,子弹），命中对象
        
        //攻击盒子变大，本质是下次检测的缩放值发生了变化
        //HitBox是否应该是一个Unit？应该不是
        //但是可以是一个Node，有坐标
        //子弹呢，子弹是一个Unit吗，是，HitBox不是是因为他更像一个流程，子弹则是一个单独的对象
        //邻域同理
        
        //是否需要状态机？不是很需要
        //
        //skill 执行时限制输入？蓄力技能的实现?技能分阶段 ability依然需要知道开始结束,把事件单独开一个周期，理解起来更清晰一些
        
        //在拥有了地图数据后并不需要使用射线检测来处理命中，直接使用地图块检索就好
        //速度方向移动是一种行为委托，地图每帧获取速度向量，更新单位的坐标
        
        //攻击 ：直接对目标产生伤害   -> single
        //      对目标周围面积产生伤害 -> aoe


----------------------------------------------------------------------------------------------
目标  2.9号完成Unit框架的基础运行逻辑
1.Ability流程完善
2.Actor系统完善，还是要接入model，使用characterController控制移动
3.战斗系统完善
4.地图块分割，筛选单位功能完善








