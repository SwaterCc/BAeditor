1.BattlrRoot 拆分出BattleManager--》管理战斗流程（初始化数据，进入战斗玩法，读取存档 ，选择模式（地图），选择队伍，加载关卡，存档数据，退出战斗玩法）

2BattleRoot 拆分出BattleLevel --》战斗关卡（地图）（初始化地图对象（BattleLevelControl，刷怪器，触发器，玩家默认出生点）波次生命周期流转（ 准备-》进行-》结算（存档）） 结算后（失败 重新开始当前波次/结算关卡，成功 执行下一波次/结算关卡）管理刷怪器的怪物列表，建筑物列表，玩家列表

3.BattleLevelControl 管理波次生命周期，关卡规则配置（总共几波，波次的准备时长，成功结算条件（时间，敌人全部死亡，自定义Ability）失败结算（时间，我方全部阵亡，自定义Ability），每波关联的刷怪器和刷怪配置传递，触发器动态激活，每波进行中默认为刷怪，也可为自定义Ability需要提供对应的API） 这些数据存储在Scene里

4.仇恨还没写完，队伍管理差相机，auto输入差归位

4.ActorManager Uid分段 1默认为关卡管理者 静态Actor段，玩家角色段，活跃对象段（子弹召唤物怪物）

3.刷怪器，触发器都是静态Actor（之后BattleLevelControl就可以通过固定的UId直接控制每个刷怪器，每个触发器，）

5.Actor入场周期，需要等待模型加载完成后才可入场

6.位移碰撞和CharacterController接入





目前有bug，show在资源加载时已经被删除了，但是线程没有终止，需要处理

表要处理，逻辑表要细分为具体的表，然后每个类型读对应的表

表要能按列遍历（不急）
伤害ID收集（后续

lua的开发环境，不能仅靠反射


###hono,项目上实现了一个属性转换组件，加了两个属性无敌和不可选中，
改了ability的Tick添加删除，还有ActorHandle的测试脚本，if节点还有属性Command，表也改了