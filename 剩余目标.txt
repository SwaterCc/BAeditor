1.触发器：指定目标进入后发送事件 （继承后，进入，）
1.刷怪器 : 
动态布怪点（抽象，纯逻辑，一次性），
静态布怪点（属于地图，可被调用，盒子名固定msg）
-> 数据来源一样，布怪器表 (配置id，怪物列表，buff列表)
首先生成盒子，盒子内部添加刷怪原点（默认在盒子中心刷），


静态可布置的Actor


2.简单AI模式（Input类），主动走向目标，攻击仇恨目标，仇恨目标切换规则，队友跟随，hateComp



3.队伍管理器，领队，领队切换

5.波次存档

6.GameMode（敌人全部死亡）





event


目前有bug，show在资源加载时已经被删除了，但是线程没有终止，需要处理

表要处理，逻辑表要细分为具体的表，然后每个类型读对应的表

表要能按列遍历（不急）
伤害ID收集（后续

lua的开发环境，不能仅靠反射


###hono,项目上实现了一个属性转换组件，加了两个属性无敌和不可选中，
改了ability的Tick添加删除，还有ActorHandle的测试脚本，if节点还有属性Command，表也改了