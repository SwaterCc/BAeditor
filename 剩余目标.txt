编辑器下拉框优化

对于Variable和Attr的获取简化

保留getAttr和GetVariable 主要用来获取其他对象身上的属性和Variable

事件系统

Actor间通信

Actor内部通信

伤害ID收集

tag限制去掉

复制节点-编辑器

确认按钮大一点


属性生成器要一个属性枚举->属性类型的记录，属性初始值记录

目前有bug，show在资源加载时已经被删除了，但是线程没有终止，需要处理

表要处理，逻辑表要细分为具体的表，然后每个类型读对应的表

表要能按列遍历

lua的开发环境，不能仅靠反射

特效对象处理

###hono,项目上实现了一个属性转换组件，加了两个属性无敌和不可选中，
改了ability的Tick添加删除，还有ActorHandle的测试脚本，if节点还有属性Command，表也改了