变量下拉框

复制节点-编辑器

tag限制去掉

Actor父子关系

现在函数难受的问题其实是在于函数调用没有强类型约束


确认按钮大一点

特效对象处理（新逻辑，新的创建方法，接受特效路径，生存时间


目前有bug，show在资源加载时已经被删除了，但是线程没有终止，需要处理

表要处理，逻辑表要细分为具体的表，然后每个类型读对应的表

表要能按列遍历（不急）
伤害ID收集（后续

lua的开发环境，不能仅靠反射


###hono,项目上实现了一个属性转换组件，加了两个属性无敌和不可选中，
改了ability的Tick添加删除，还有ActorHandle的测试脚本，if节点还有属性Command，表也改了